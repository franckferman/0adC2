# Compiled binaries — never commit
agent
agent_debug
agent_verbose
ctrl
stager
stage_srv

# Polymorphic build variants
agent_poly
ctrl_poly
stager_poly
stage_srv_poly
stage_srv_std

# BPF and C object files
*.o
*.bpf.o

# Generated headers (contain random per-build IOCs or real config)
# Produced automatically by gen_*.py — make regenerates them.
src/obf_agent_iocs.h
src/obf_agent_strings.h
src/obf_strings.h
src/bpf_rootkit_obj.h
src/hide_proc_obj.h
src/poly_cfg.h
src/poly_stubs.h

# Release artifacts (generated by CI — not for VCS)
*.tar.gz
checksums.txt

# Operational files (runtime or local config)
hostname

# Python cache
__pycache__/
*.pyc
*.pyo

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Debug / scratch artifacts (from dev sessions at repo root)
test_connect
test_connect.c
